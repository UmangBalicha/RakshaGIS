/* Public site configuration.
 *
 * Placeholders (empty strings / YOUR-* values) mean "not provided yet" and
 * are never rendered to visitors. Fill them in before production launch and
 * redeploy — see the final launch checklist in the project README.
 */

/** Canonical production URL, e.g. 'https://rakshagis.in'. No trailing slash. */
export const SITE_URL = 'https://YOUR-PRODUCTION-DOMAIN';

/** Public contact email shown on the Privacy/Terms pages when set. */
export const CONTACT_EMAIL = '';

/** Physical organization address shown in the footer when set. */
export const CONTACT_ADDRESS = '';
